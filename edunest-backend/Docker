FROM maven:3.9.6-eclipse-temurin-17

WORKDIR /app

# Copy only pom first (for caching)
COPY pom.xml .

RUN mvn dependency:go-offline

# Now copy source code
COPY src ./src

# Build jar
RUN mvn clean package -DskipTests

EXPOSE 5000

CMD ["java", "-jar", "target/edunest-0.0.1-SNAPSHOT.jar"]